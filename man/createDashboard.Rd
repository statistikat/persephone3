% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createDashboard.R
\name{createDashboard}
\alias{createDashboard}
\alias{createDashboard_single}
\alias{createDashboard_hts}
\alias{createDashboard_compare}
\title{Create diagnostic dashboard for time series objects}
\usage{
createDashboard(x, y = NULL, title = NULL, file = NULL, ...)

createDashboard_single(x, title = "Series Diagnostics", file = NULL, ...)

createDashboard_hts(x, title = NULL, file = NULL, ...)

createDashboard_compare(x, y, title = "Series Comparison", file = NULL, ...)
}
\arguments{
\item{x}{A time series object (single series or hierarchical), or a list of two single series objects for comparison}

\item{y}{Second time series object (optional, for comparison of two single series)}

\item{title}{Main title for the dashboard (default depends on object type)}

\item{file}{Optional file path to save HTML output (single series only)}

\item{...}{Additional arguments passed to specific methods}
}
\value{
For single series: browsable HTML UI. For hierarchical: invisible \code{callr} process.
}
\description{
Creates a diagnostic dashboard for time series objects. For single series
objects, returns an HTML dashboard. For hierarchical time series, launches
an interactive Shiny application. Can also compare two single series objects.
}
\section{Functions}{
\itemize{
\item \code{createDashboard_single()}: Internal helper for single series dashboards

\item \code{createDashboard_hts()}: Internal helper for hierarchical time series dashboards

\item \code{createDashboard_compare()}: Internal helper for comparing two single series dashboards

}}
\examples{
# For single series - returns HTML dashboard
obj <- perX13(AirPassengers, "rsa3")
obj$run()
createDashboard(obj)

# Compare two series
obj1 <- perX13(AirPassengers, "rsa3")
obj1$run()
obj2 <- perX13(AirPassengers, "rsa1")
obj2$run()
createDashboard(obj1, obj2, title = "rsa3 vs rsa1")

# For hierarchical series - launches Shiny app
# createDashboard(hts_object)
}
