% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perAgent.R
\name{perAgentOpenAI}
\alias{perAgentOpenAI}
\title{Create an OpenAI-backed decider for \code{\link[=perAgent]{perAgent()}}}
\usage{
perAgentOpenAI(
  model = "gpt-4.1-mini",
  credentials = NULL,
  api_key = NULL,
  system_prompt = NULL,
  base_url = "https://api.openai.com/v1"
)
}
\arguments{
\item{model}{OpenAI model name passed to \code{ellmer::chat_openai()}.}

\item{credentials}{Optional credential provider passed to
\code{ellmer::chat_openai()}. It should be a zero-argument function that returns
either an API key string or a named list of headers.}

\item{api_key}{Optional API key kept for backwards compatibility. When
supplied, it is converted to an \code{ellmer} \code{credentials} function internally.}

\item{system_prompt}{Optional extra instructions appended to the built-in
system prompt.}

\item{base_url}{Base URL passed to \code{ellmer::chat_openai()}.}
}
\value{
A function suitable for the \code{llm_decider} argument of \code{\link[=perAgent]{perAgent()}}.
}
\description{
This helper creates a small adapter around \code{ellmer::chat_openai()} that can
be supplied to \code{perAgent(llm_decider = ...)}. The language model does not
create new candidate updates by itself; it only prioritises one of the
already generated candidate actions, or proposes a small custom update.
}
\details{
By default, authentication is delegated to \code{ellmer}, which means that
setting \code{OPENAI_API_KEY} is the recommended approach.
}
\examples{
decider <- perAgentOpenAI()
is.function(decider)
}
