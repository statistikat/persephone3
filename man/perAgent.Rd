% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perAgent.R
\name{perAgent}
\alias{perAgent}
\title{ESS-inspired agentic seasonal adjustment}
\usage{
perAgent(
  ts,
  method = c("auto", "x13", "tramo", "tramoseats"),
  template = NULL,
  context = NULL,
  userdefined = NULL,
  speclist = NULL,
  max_iter = 5,
  alpha = 0.05,
  improvement_threshold = 0.5,
  allow_no_adjustment = TRUE,
  llm_decider = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{ts}{A univariate time series.}

\item{method}{One of \code{"auto"}, \code{"x13"}, \code{"tramo"} or \code{"tramoseats"}.
In \code{"auto"} mode both X-13ARIMA-SEATS and TRAMO-SEATS are run with the
same starting specification and the better-scoring candidate is used as the
starting point for the iterative search.}

\item{template}{The initial specification template. Defaults to \code{"rsa3"}.
When \code{method = "auto"}, templates that are unsupported by one of the two
methods are simply skipped for that method.}

\item{context}{Passed to \code{\link[=perX13]{perX13()}} or \code{\link[=perTramo]{perTramo()}}.}

\item{userdefined}{Passed to \code{\link[=perX13]{perX13()}} or \code{\link[=perTramo]{perTramo()}}.}

\item{speclist}{Optional named list of initial specification updates.}

\item{max_iter}{Maximum number of accepted improving iterations after the
initial run.}

\item{alpha}{Significance level used in the ESS-inspired decision rules.}

\item{improvement_threshold}{Minimum score improvement that is required to
accept a new candidate.}

\item{allow_no_adjustment}{If \code{TRUE}, the result records when the pre-tests do
not identify seasonality clearly enough for publishing adjusted data.}

\item{llm_decider}{Optional function that can prioritise the next candidate
action. The function receives a compact state list and should return either
a candidate action name or a list with at least an \code{action} element and,
optionally, \code{updates}, \code{method}, and \code{rationale}.}

\item{verbose}{Passed to the underlying \verb{$run()} method.}

\item{...}{Additional specification updates passed to the underlying
specification builder.}
}
\value{
An object of class \code{persephoneAgent}.
}
\description{
\code{perAgent()} orchestrates repeated runs of \code{\link[=perX13]{perX13()}} and/or
\code{\link[=perTramo]{perTramo()}}, starting from the default seasonal-adjustment template and
iteratively updating the specification when the diagnostics suggest that an
improvement is possible. The implementation is intentionally conservative
and mirrors a subset of the ESS guidance:
}
\details{
\itemize{
\item test for seasonality before adjustment;
\item compare X-13ARIMA-SEATS and TRAMO-SEATS when the method is set to \code{"auto"};
\item validate residual seasonality and trading-day effects after each run;
\item prefer small, explainable specification changes over large jumps.
}

The returned object keeps the full evaluation history together with the best
final model.
}
\examples{
res <- perAgent(AirPassengers)
res

# Fix the method instead of selecting it automatically
res_x13 <- perAgent(AirPassengers, method = "x13")
res_x13$selected_method
}
